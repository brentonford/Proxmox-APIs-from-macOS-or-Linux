Creating an API token requires specifying a user and token ID. Via the command line:

`pveum user token add root@pam automation --privsep 0`

The token secret is displayed only once—store it securely.